# Reports Output Directory

This is the default directory where APHI exports analysis reports.

When a user clicks "Export Report" in the APHI dockpane, they can choose between HTML, CSV, JSON, or Text formats. 

If you are a developer testing the report generation engine, your test outputs will be saved here by default (unless overridden in the configuration settings).

Files in this directory should generally not be committed to version control.
