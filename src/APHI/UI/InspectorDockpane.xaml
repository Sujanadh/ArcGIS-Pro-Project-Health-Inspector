<UserControl x:Class="APHI.UI.InspectorDockpaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:APHI.UI"
             xmlns:converters="clr-namespace:APHI.UI.Converters"
             xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/InspectorStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <converters:SeverityToIconConverter x:Key="SeverityToIconConverter" />
            <converters:SeverityToColorConverter x:Key="SeverityToColorConverter" />
            <converters:ScoreToColorConverter x:Key="ScoreToColorConverter" />
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource InspectorCardStyle}">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Command="{Binding OpenSettingsCommand}" ToolTip="Settings" Style="{StaticResource InspectorSecondaryButtonStyle}">⚙</Button>
                </StackPanel>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{Binding ProjectName}" FontWeight="Bold" FontSize="16" />
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <TextBlock Text="Health Score: " />
                        <Border Background="{Binding HealthScore, Converter={StaticResource ScoreToColorConverter}}" CornerRadius="10" Padding="5,2">
                            <TextBlock Text="{Binding HealthScore}" Foreground="White" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Toolbar / Scan Controls -->
        <Border Grid.Row="1" Style="{StaticResource InspectorCardStyle}">
            <StackPanel Orientation="Vertical">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Scan Project" Command="{Binding ScanCommand}" Style="{StaticResource InspectorPrimaryButtonStyle}" Margin="0,0,5,0"/>
                        <Button Content="Cancel" Command="{Binding CancelScanCommand}" Style="{StaticResource InspectorDangerButtonStyle}" 
                                Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="HTML" Command="{Binding ExportHtmlCommand}" Style="{StaticResource InspectorSecondaryButtonStyle}" Margin="2,0"/>
                        <Button Content="CSV" Command="{Binding ExportCsvCommand}" Style="{StaticResource InspectorSecondaryButtonStyle}" Margin="2,0"/>
                    </StackPanel>
                </Grid>
                
                <ProgressBar Value="{Binding ScanProgress}" Margin="0,10,0,0" 
                             Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibilityConverter}}"
                             Style="{StaticResource InspectorProgressBarStyle}"/>
                <TextBlock Text="{Binding ProgressMessage}" Margin="0,2,0,0"
                           Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>
        </Border>

        <!-- Main Content (Tabs & Grid) -->
        <TabControl Grid.Row="2" SelectedIndex="{Binding SelectedTabIndex}" Margin="5">
            <TabItem Header="All Issues">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Filters -->
                    <DockPanel Grid.Row="0" Margin="0,0,0,5">
                        <controls:SearchTextBox DockPanel.Dock="Left" Width="150" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" InfoText="Search issues..."/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="Severity:" VerticalAlignment="Center" Margin="5,0"/>
                            <ComboBox SelectedItem="{Binding SelectedSeverityFilter}" Width="100">
                                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">All</System:String>
                                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">Critical</System:String>
                                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">High</System:String>
                                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">Medium</System:String>
                                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">Low</System:String>
                                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">Information</System:String>
                            </ComboBox>
                        </StackPanel>
                    </DockPanel>

                    <!-- DataGrid -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding FilteredIssues}" SelectedItem="{Binding SelectedIssue}" 
                              Style="{StaticResource InspectorDataGridStyle}">
                        <DataGrid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="View Details" />
                                <MenuItem Header="Copy" Command="{Binding CopyIssueCommand}" />
                                <MenuItem Header="Auto-Fix" Command="{Binding AutoFixSelectedCommand}" />
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Sev">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding Severity, Converter={StaticResource SeverityToIconConverter}}" Width="16" Height="16" Margin="2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                            <DataGridTextColumn Header="Category" Binding="{Binding CategoryDisplayName}" Width="100" />
                            <DataGridTextColumn Header="Item" Binding="{Binding AffectedItem}" Width="120" />
                            <DataGridCheckBoxColumn Header="Fixable" Binding="{Binding IsAutoFixable}" IsReadOnly="True" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="By Category">
                <TextBlock Text="Category View Placeholder" Margin="10"/>
            </TabItem>
            <TabItem Header="Performance">
                <TextBlock Text="Performance View Placeholder" Margin="10"/>
            </TabItem>
            <TabItem Header="Recommendations">
                <TextBlock Text="Recommendations View Placeholder" Margin="10"/>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Total Issues: " />
                    <TextBlock Text="{Binding Issues.Count}" FontWeight="Bold" />
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
